@model InformationSystemDomain.Model.AuthorsPerArticle

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>AuthorsPerArticle</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="LinkId" />
            <div class="form-group">
                <label asp-for="Authors" class="control-label"></label>
                <select asp-for="Authors" class="form-control" asp-items="ViewBag.Authors"></select>
                <span asp-validation-for="Authors" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Article" class="control-label"></label>
                <select asp-for="Article" class="form-control" asp-items="ViewBag.Article"></select>
                <span asp-validation-for="Article" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
